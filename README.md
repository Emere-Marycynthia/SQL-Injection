# SQL-Injection
This lab highlights the impact of improper input validation and weak database security controls, demonstrating how attackers can gain unauthorized access to sensitive data if applications are not properly secured.
